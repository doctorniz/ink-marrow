/**
 * @deprecated Replaced by the real in-document chat panel
 * (`src/components/chat/chat-panel.tsx`). This file is kept only
 * because the sandboxed workspace cannot delete files — the export
 * below is an empty component so stale imports compile, and it is
 * no longer wired into `AppShell`. Safe to delete from a real
 * checkout.
 */
export function ComingSoonChat() {
  return null
}
